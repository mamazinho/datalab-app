import { Input } from '../../../styles/design-system.style';
import styled from 'styled-components';

export const CodeInput = styled(Input)`
    text-align: center;
    letter-spacing: 0.35rem;
    font-size: 1.05rem;
`;

export const Actions = styled.div`
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
    margin-top: 0.5rem;
`;
